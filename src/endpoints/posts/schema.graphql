type Post {
  id: ID!
  title: String!
  text: String!
  isPublished: Boolean!
}

input NewPost {
  title: String!
  text: String!
}

input UpdatePost {
  id: ID!
  title: String
  text: String
  isPublished: Boolean
}
